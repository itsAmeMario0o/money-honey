fastapi==0.135.3
# starlette is left to fastapi's own pin (<0.39, >=0.37.2). The
# CVE-2024-47874 multipart-form DoS is ignored in .trivyignore.yaml
# because our /api/chat endpoint accepts JSON only — no multipart
# parser is exposed. Re-evaluate when we bump fastapi to 0.116+
# which allows starlette 0.40+.
uvicorn[standard]==0.44.0
pydantic==2.13.0
python-dotenv==1.2.2
# langchain ecosystem — sub-packages version tightly with langchain-core.
# We pin langchain + langchain-community to exact patches (CVE fix) and
# allow the smaller sub-packages to float within their compatible minors
# so pip's resolver can pick a consistent langchain-core without needing
# a lock file. Stays on the 0.3 line; 1.x would require bigger changes.
langchain>=0.3.27,<0.4.0
langchain-community>=0.3.27,<0.4.0
langchain-anthropic>=0.3.15,<0.4.0
langchain-huggingface>=0.3.1,<0.4.0
sentence-transformers==3.3.1
faiss-cpu==1.13.2
pypdf==5.1.0

# --- dev / test only ---
# These live here (not in a separate requirements-dev.txt) for simplicity.
# The Dockerfile uses pip with --no-deps filtering via a constraints
# pattern later if needed; for now the deploy image installs everything
# — the size hit is small.
black==26.3.1
ruff==0.15.10
mypy==1.20.1
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
